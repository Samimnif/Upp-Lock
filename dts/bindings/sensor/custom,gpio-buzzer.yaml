description: Simple GPIO buzzer

compatible: "custom,gpio-buzzer"

include: [base.yaml]

properties:
  gpios:
    type: phandle-array
    required: true
    description: GPIO used to drive the buzzer.
